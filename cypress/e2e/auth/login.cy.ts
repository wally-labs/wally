describe("Login with Email & Password", () => {
  it("logs in successfully", () => {
    return;
  });

  it("fails with the wrong password", () => {
    return;
  });

  it("fails with the wrong email", () => {
    return;
  });
});

describe("Login with Google Auth", () => {
  it("redirects to Google portal", () => {
    return;
  });
});

describe("Login with Apple Auth", () => {
  it("redirects to Apple portal", () => {
    return;
  });
});

describe("Login with Microsoft Auth", () => {
  it("redirects to Microsoft portal", () => {
    return;
  });
});
